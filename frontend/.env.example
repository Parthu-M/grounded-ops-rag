# Use demo for a standalone, key-free UI or live for a connected FastAPI deployment.
VITE_DEFAULT_API_MODE=demo

# Leave empty when the frontend is served by FastAPI. For a separate frontend,
# set this to the API origin, for example http://localhost:8000.
VITE_API_BASE_URL=
